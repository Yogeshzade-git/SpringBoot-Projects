
spring:
  mail:
    host: smtp.gmail.com  # Suggests valid properties like 'host'
    port: 587              # Suggests valid values based on context
    username: ymzade2022@gmail.com
    password: ${spring.mail.password}     // you have to create a app password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB


